{
  "name": "add-gql-router",
  "version": "1.0.1",
  "description": "给前端graphql请求增加一层可识别路由",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "graphql"
  ],
  "author": "wangliran",
  "license": "ISC",
  "dependencies": {
    "eslint": "^5.16.0"
  }
}